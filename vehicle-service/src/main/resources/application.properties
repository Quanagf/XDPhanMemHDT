# Service này sẽ chạy trên cổng 8080 (bên trong Docker)
server.port=8080

# --- Cấu hình MySQL ---
spring.datasource.url=jdbc:mysql://mysql-db:3306/vehicle_db?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD}

# --- Cấu hình JPA ---
# Tự động tạo/cập nhật bảng
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Minio configuration (used for vehicle images)
minio.endpoint=http://minio:9000
minio.access-key=minioadmin
minio.secret-key=minioadminpassword
minio.bucket-name=ev-rental