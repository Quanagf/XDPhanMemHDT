server:
  port: 8085 # <-- Cổng mới

spring:
  application:
    name: notification-service # <-- Tên mới
  
  rabbitmq:
    host: rabbitmq
  
  datasource:
    url: jdbc:mysql://mysql-db:3306/notificationdb?createDatabaseIfNotExist=true # <-- DB mới
    username: root
    password: Quan11092005@
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/